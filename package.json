{
  "name": "x-split-reader",
  "version": "0.1.0",
  "private": true,
  "description": "Scroll-driven master-detail reader for X",
  "scripts": {
    "check": "node --check x-split-reader.user.js",
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}


{
  "name": "x-split-reader",
  "version": "0.2.0",
  "private": true,
  "description": "Compact split reader for X with fixed-width timeline and adaptive detail pane",
  "scripts": {
    "check": "node --check x-split-reader.user.js",
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
